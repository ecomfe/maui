{
  "name": "@maui/core",
  "version": "0.8.0",
  "description": "Core module of maui, supports information and action management",
  "type": "module",
  "engines": {
    "node": ">=v18.20.0"
  },
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint --max-warnings=0 --fix src",
    "test": "vitest run --coverage"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@types/json-schema": "^7.0.15"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^1.4.0",
    "vitest": "^1.4.0"
  }
}
